flask==3.0.3
opencv-python-headless==4.10.0.84
numpy==1.26.4
pillow==10.4.0
requests==2.32.3
tensorflow==2.17.0
# Only needed for the optional cloud AI backend (DETECTION_BACKEND=cloud) -
# left unpinned to a specific patch since the SDK moves quickly; run
# `pip install -U anthropic` if you hit an incompatibility.
anthropic>=0.40.0
# Only needed for the optional local YOLOv8 backend (DETECTION_BACKEND=yolo).
# Pulls in torch automatically; installs CPU-only by default - see README
# "YOLOv8 backend" for GPU setup.
ultralytics>=8.2.0
